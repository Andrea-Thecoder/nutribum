export const COLS = 12;
export const ROW_HEIGHT = 30;

export const CAP_ALTEZZA_PX = 380;

// Passato a gridConfig di ReactGridLayout: margine in pixel tra un pannello e l'altro.
export const MARGIN: [number, number] = [12, 12];
